export class ListadoPokemones {
  constructor(total, siguienteURL, anteriorURL, nombresDePokemones) {
    this.total = total;
    this.siguienteURL = siguienteURL;
    this.anteriorURL = anteriorURL;
    this.nombresDePokemones = nombresDePokemones;
  }
}
